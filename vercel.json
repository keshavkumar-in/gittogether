{
  "version": 2,
  "build": {
    "env": {
      "NODE_ENV": "production"
    },
    "commands": [
      "npm install",
      "npm run build"
    ],
    "outputDirectory": "dist"
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}